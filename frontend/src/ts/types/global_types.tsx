export type TMenuLainnya = {
  menu: string;
  icon: React.ReactNode;
  path: string;
};
export type TMenuUtama = {
  menu: string;
  icon: React.ReactNode;
  path: string;
};

export type TShowTotalAllCount = {
  title: string;
  total: number;
};
export type TErrors = {
  email?: string[];
  password?: string[];
  message?: string[];
};

export type TServices = {
  menu: string;
  slug: string;
};
